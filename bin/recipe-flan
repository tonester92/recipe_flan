#!/usr/bin/env ruby

require "bundler/setup"
require_relative "../lib/recipe_flan"

RecipeFlan::CLI.new.call

#created recipe flan with the name space opertator because it gives access to a different classes and my class is CLI
#I choose a the .new method because it is a class instance that instantiates a new class which I called CLI.  